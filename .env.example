# Copy to .env and adjust. All values shown are the defaults baked into
# docker-compose.yml — the stack works with no .env at all.

KAFKA_PORT=9092

QUESTDB_HTTP_PORT=9000
QUESTDB_ILP_PORT=9009
QUESTDB_PG_PORT=8812

PG_PORT=5432
PG_USER=fleet
PG_PASSWORD=fleet
PG_DATABASE=fleet

GRAFANA_PORT=3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=fleet
